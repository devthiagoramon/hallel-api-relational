MAIL_PASSWORD: uvbylzgyrbasdvte
MAIL_USERNAME: miguelimabr10@gmail.com
security:
  jwt:
    token:
      expire-length: 2592000000
      expire-coordenador-length: 86400000
firebase:
  messaging:
    fcm:
      token: AIzaSyB-ym4UH0PQahpZM9XzrrqCeQsJwnJdIos
      url: https://fcm.googleapis.com/v1/projects/hallel-messaging/messages:send
spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    open-in-view: false
  cloud:
    gcp:
      storage:
        bucket: hallel-bucket
  task:
    scheduling:
      cron: 0 00 * * * ?
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    protocol: smtp
    default-encoding: UTF-8
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
  application:
    name: hallel-api
  profiles:
    active: dev
  docker:
    compose:
      enabled: false
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false
  servlet:
    multipart:
      max-request-size: 10MB
      max-file-size: 10MB
server:
  port: 8080
springdoc:

  api-docs:
    path: "/v3/api-docs"
  swagger-ui:
    path: "/swagger-ui.html"
    enabled: true

